import 'package:flutter/material.dart';

class StoriesCarousel extends StatelessWidget {
  const StoriesCarousel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
