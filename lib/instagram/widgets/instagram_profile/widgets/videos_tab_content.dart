import 'package:flutter/material.dart';

class VideosTabContent extends StatelessWidget {
  const VideosTabContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
