part of widgets;

class VerticalSpace extends SizedBox {
  const VerticalSpace(double value, {Key? key}) : super(key: key, height: value);
}

class HorizontalSpace extends SizedBox {
  const HorizontalSpace(double value, {Key? key}) : super(key: key, width: value);
}
